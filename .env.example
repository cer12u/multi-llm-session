# Generate fresh local values with: node deploy/init-env.mjs
# Do not paste credentials into issues, source control, or chat.
ADMIN_TOKEN=
WORKER_A_TOKEN=
WORKER_B_TOKEN=
WORKER_C_TOKEN=
# Live model settings are deliberately absent from the default Compose deployment.
